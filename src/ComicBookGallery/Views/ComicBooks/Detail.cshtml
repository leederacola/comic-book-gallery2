@model ComicBookGallery.Models.ComicBook
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = Model.DisplatText;

}

<!DOCTYPE html>


<h1>@Model.SeriesTitle</h1>
<h2>#@Model.IssueNumber</h2>

<h5>Description:</h5>
<div>@Html.Raw(Model.DescriptionHtml)</div>
@if (Model.Artist.Length > 0)
{
    <h5>Artist:</h5>
    <div>
        <ul>
            @foreach (var artist in Model.Artist)
            {
                <li>@artist.Role: @artist.Name</li>
            }
        </ul>
    </div>
}

